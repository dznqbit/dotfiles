# aliases.zsh
alias resource="source ~/.zshrc"

alias magerun="n98-magerun.phar"          # PHP Magerun

alias ag=rg                               # Ripgrep 
alias pg="ps aux | grep $1"               # List processes by name
alias be="bundle exec"
alias begs="bundle exec guard start"

alias gd="git diff"
alias gdc="git diff --cached"
alias gcm="git commit -m $1"              # Git commit with message
